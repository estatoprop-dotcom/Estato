services:
  - type: web
    name: estato-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      # Add your Supabase keys in Render Dashboard → Environment Variables
      # DO NOT add sensitive keys here - use Render Dashboard instead

