{
  "name": "estato",
  "version": "1.0.0",
  "description": "Estato Real Estate App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.86.0",
    "axios": "^1.13.2",
    "form-data": "^4.0.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
